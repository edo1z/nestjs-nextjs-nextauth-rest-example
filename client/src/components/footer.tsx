import { Box, Divider, Container } from "@chakra-ui/react";

export default function Footer() {
  return (
    <Box mt={20}>
      <Divider />
      <Container pt={5}>
        <div>Footer</div>
      </Container>
    </Box>
  );
}